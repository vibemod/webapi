---
description: Run all QA checks (code sniffer, PHPStan, and PHPUnit)
tags: [qa, testing, quality, phpstan, codesniffer, phpunit]
---

Run all quality assurance checks: code sniffer (`make cs`), PHPStan static analysis (`make phpstan`), and PHPUnit tests (`make test`). Report any errors or failures found.